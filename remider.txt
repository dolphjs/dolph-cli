there are two types  of projects: 
and the large only that supports the CLI options

- the large and
- the small


update to cli-colors or chalk

In the future, one config file should do, chnage directry name to something liek this: user.controller.ts

limit call  of readConfig to speed up the process

new command for initing config files
add dolph build and test for javascript files
add more details in the stat command
update readme to tell user they need to install ts-node and swc